# Local development only. Do not commit deployed backend URLs here.
# In Firebase App Hosting, set ADK_BACKEND_URL in runtime configuration for each deployed environment.
ADK_BACKEND_URL=http://localhost:8000